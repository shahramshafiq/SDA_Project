package com.hotel.chatbot.chatbot;

/**
 * Enum representing the intents that the chatbot can detect from user messages.
 */
public enum Intent {
    CHECK_AVAILABILITY,
    BOOK_ROOM,
    CANCEL_BOOKING,
    LIST_BOOKINGS,
    GREETING,
    HELP,
    UNKNOWN
}
